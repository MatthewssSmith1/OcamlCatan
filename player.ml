type t = {
  hand : Game_state.resource list;
  devs : Game_state.devCard list;
  settlements : int;
  cities : int;
  roads : int;
}

let add_resource = failwith "todo"

let remove_resource = failwith "todo"

let check_resource = failwith "todo"

let add_port = failwith "todo"
